function myFunction(str) {
    
    

    for(var i = 0; i < str.length; i++) {
        let = resu = String.fromCharCode(str.charCodeAt(0) + 1)
    }
    console.log(resu);
}

myFunction('bnchmf')
myFunction('bgddrd')
